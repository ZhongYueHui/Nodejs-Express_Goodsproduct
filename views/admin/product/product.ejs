<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title></title>
</head>

<body>
    <!--The content below is only a placeholder and can be replaced.-->

    <link rel="stylesheet" href="/bootstrap/css/bootstrap.css">

    <!-- 公共的头部 -->
    <%include ../pubilc/header.ejs%>
    <!-- 公共的侧边栏 -->
    <%include ../pubilc/aslideleft.ejs%>
    <div class="col-sm-10">
        <ol class="breadcrumb">
            <li class="active">商品管理
            </li>
            <li class="active">商品列表
            </li>
        </ol>
        <div class="panel panel-default">
            <div class="panel-heading">
                搜索
            </div>
            <div class="panel-body">
                <form role="form" class="form-inline">
                    <div class="form-group">
                        <label for="name">名称</label>
                        <input type="text" class="form-control" id="name" placeholder="请输入名称">
                    </div>

                    <div class="form-group">
                        <button type="submit" class="btn btn-default">开始搜索</button>
                    </div>
                </form>
            </div>
        </div>
        <!--
                列表展示
            -->
        <div class="table-responsive">
            <table class="table table-striped ">
                <thead>
                    <tr>
                        <th>编号</th>
                        <th>图标</th>
                        <th>名称</th>
                        <th>价格</th>
                        <th>邮费</th>

                        <th class="text-center">操作</th>
                    </tr>
                </thead>
                <tbody>

                    <% for(let i=0;i<List.length;i++){%>

                    <tr>
                        <td><%=i+1%></td>
                        <td><img src="/<%=List[i].image%>" style="width: 20px;height: 20px"></td>
                        <td><%=List[i].tradename%></td>
                        <td><%=List[i].price%></td>
                        <td><%=List[i].postage%></td>

                        <td class="text-center"><a href="/admin/product/doproductedit?id=<%=List[i]._id%>"
                                class="btn btn-success">修改
                            </a> <a href="/admin/product/deleteproduct?id=<%=List[i]._id%>" class="btn btn-danger">
                                删除</a>
                        </td>
                    </tr>

                    <%}%>
                </tbody>
            </table>
        </div>

    </div>
    </div>
    </div>

</body>

</html>